---
import Hero from '~/components/widgets/Hero.astro';
import Layout from '~/layouts/PageLayout.astro';
import certificate from '~/assets/images/certificate-red-movie-awards.jpg';
import Image from '~/components/common/Image.astro';

const fetivalTitle = 'Red Movie Awards';

const metadata = {
  title: fetivalTitle
};
---

<Layout metadata={metadata}>
  <Hero tagline={fetivalTitle}>
    <Fragment slot="title">Best Editor</Fragment>

    <Fragment slot="image">
      <div class="relative">
        <Image src={certificate} alt=`${fetivalTitle}: Certificate` width={566} height={800} />
      </div>
    </Fragment>
  </Hero>
</Layout>
